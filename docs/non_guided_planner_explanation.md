# Non-Guided Planner 详解

## 📋 问题：什么是 Non-Guided Planner？

在 T-MPC 中，**non-guided planner**（非引导规划器）是指**不使用引导轨迹约束的 MPC 求解器**，也称为 **T-MPC++**。

---

## 🎯 核心概念对比

### **Guided Planner（引导规划器）**

```
┌─────────────────────────────────────────────────┐
│         Guided Planner (引导规划器)              │
├─────────────────────────────────────────────────┤
│                                                 │
│  1. 从全局引导规划器获取引导轨迹                  │
│     └─ 例如：从障碍物左侧绕过的轨迹              │
│                                                 │
│  2. 用引导轨迹初始化 MPC 求解器                  │
│     └─ 设置初始猜测 (warm start)                │
│                                                 │
│  3. 添加拓扑约束 (Topology Constraints)         │
│     └─ 强制轨迹保持在引导轨迹附近                │
│     └─ 确保不跳到其他拓扑类别                    │
│                                                 │
│  4. MPC 优化                                    │
│     └─ 在引导轨迹的"管道"内优化                  │
│     └─ 局部最优解（特定拓扑类别）                │
│                                                 │
└─────────────────────────────────────────────────┘
```

### **Non-Guided Planner（非引导规划器）**

```
┌─────────────────────────────────────────────────┐
│      Non-Guided Planner (非引导规划器)           │
│              T-MPC++                            │
├─────────────────────────────────────────────────┤
│                                                 │
│  1. 不使用任何引导轨迹                           │
│     └─ 完全自由优化                              │
│                                                 │
│  2. 只有安全约束                                 │
│     └─ 避障约束                                  │
│     └─ 动力学约束                                │
│     └─ 控制输入约束                              │
│                                                 │
│  3. 没有拓扑约束                                 │
│     └─ 可以自由选择任何路径                      │
│     └─ 不受引导轨迹限制                          │
│                                                 │
│  4. MPC 优化                                    │
│     └─ 在整个可行空间内优化                      │
│     └─ 可能找到引导规划器未考虑的路径            │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## 🔍 代码中的体现

### 1. **创建 Non-Guided Planner**

<augment_code_snippet path="mpc_planner_modules/src/guidance_constraints.cpp" mode="EXCERPT">
```cpp
// 构造函数中
if (_use_tmpcpp)
{
    LOG_INFO("Using T-MPC++ (Adding the non-guided planner in parallel)");
    planners_.emplace_back(n_solvers, true);  // is_original_planner = true
}
```
</augment_code_snippet>

**关键参数**：
- `is_original_planner = true` → 标记为非引导规划器
- 这个 planner 不会被分配引导轨迹

---

### 2. **Non-Guided Planner 的约束构建**

<augment_code_snippet path="mpc_planner_modules/src/guidance_constraints.cpp" mode="EXCERPT">
```cpp
// 在 optimize() 函数中
if (planner.is_original_planner || (!_enable_constraints))
{
    // 非引导规划器：使用空数据（无引导约束）
    planner.guidance_constraints->update(state, empty_data_, module_data);
    planner.safety_constraints->update(state, data, module_data);
}
else
{
    // 引导规划器：使用引导轨迹初始化
    initializeSolverWithGuidance(planner);
    planner.guidance_constraints->update(state, data, module_data);
    planner.safety_constraints->update(state, data, module_data);
}
```
</augment_code_snippet>

**关键区别**：
- **Non-guided**: `update(state, empty_data_, module_data)` → 空数据，无引导约束
- **Guided**: `update(state, data, module_data)` → 包含引导轨迹数据

---

### 3. **拓扑约束的实现**

<augment_code_snippet path="mpc_planner_modules/src/linearized_constraints.cpp" mode="EXCERPT">
```cpp
void LinearizedConstraints::setTopologyConstraints()
{
    _n_discs = 1;           // 只用一个圆盘（简化）
    _use_guidance = true;   // 启用引导模式
}

void LinearizedConstraints::update(...)
{
    if (!_use_guidance)  // 非引导模式
    {
        // 使用机器人的多个圆盘
        auto &disc = data.robot_area[d];
        // 计算每个圆盘的位置
        // 使用真实的障碍物半径
        double radius = obstacle.radius;
    }
    else  // 引导模式（拓扑约束）
    {
        // 只使用机器人中心点
        // 使用极小的半径（几乎是点约束）
        double radius = 1e-3;  // 0.001 米
    }
}
```
</augment_code_snippet>

**拓扑约束的作用**：
- 将机器人简化为一个点（`radius = 1e-3`）
- 在引导轨迹周围构建线性约束
- 形成一个"管道"，限制轨迹不能偏离太远

---

### 4. **Non-Guided Planner 的标识**

<augment_code_snippet path="mpc_planner_modules/src/guidance_constraints.cpp" mode="EXCERPT">
```cpp
if (planner.is_original_planner)  // 非引导规划器
{
    // 设置一个特殊的 guidance_ID（高于所有拓扑类别）
    planner.result.guidance_ID = 2 * global_guidance_->GetConfig()->n_paths_;
    planner.result.color = -1;  // 无颜色（可视化时区分）
}
else  // 引导规划器
{
    // 使用引导轨迹的拓扑类别 ID
    auto &guidance_trajectory = global_guidance_->GetGuidanceTrajectory(planner.id);
    planner.result.guidance_ID = guidance_trajectory.topology_class;
    planner.result.color = guidance_trajectory.color_;
}
```
</augment_code_snippet>

---

## 🎨 可视化对比

### **场景示例：机器人需要绕过障碍物**

```
起点 S ────────────────────────────────────────► 终点 G
                    ⬤ 障碍物
```

#### **Guided Planners（4 个）**

```
Planner 0 (Homotopy Class 1): 从障碍物上方绕过
    S ─────╮
           │
           ╰──► ⬤ ──────────► G
           
Planner 1 (Homotopy Class 2): 从障碍物下方绕过
    S ──────────► ⬤ ──╮
                      │
                      ╰──────► G
           
Planner 2 (Homotopy Class 3): 从障碍物左侧绕过
    S ──╮
        │
        ╰─► ⬤ ────────────────► G
           
Planner 3 (Homotopy Class 4): 从障碍物右侧绕过
    S ──────────────► ⬤ ──╮
                          │
                          ╰──► G
```

每个 Guided Planner：
- ✅ 有明确的引导轨迹
- ✅ 受拓扑约束限制
- ✅ 只能在引导轨迹附近优化
- ❌ 不能跳到其他拓扑类别

#### **Non-Guided Planner（1 个）**

```
Planner 4 (T-MPC++): 自由优化
    S ──────────────────────────────► G
         可能选择任何路径！
         
    可能的结果：
    - 直线路径（如果障碍物移开）
    - 最短路径（可能与引导轨迹不同）
    - 创新路径（引导规划器未考虑的）
```

Non-Guided Planner：
- ✅ 完全自由优化
- ✅ 可以找到引导规划器未考虑的路径
- ✅ 适应动态变化的环境
- ❌ 可能收敛较慢（无初始猜测）
- ❌ 可能陷入局部最优

---

## 🚀 T-MPC++ 的优势

### **为什么需要 Non-Guided Planner？**

#### **1. 引导规划器的局限性**

引导规划器依赖于**全局引导规划**（PRM + 路径搜索）：

```
问题：
- 引导规划是离线或低频更新的
- 可能无法适应快速变化的动态环境
- 可能遗漏某些可行路径
- 拓扑约束可能过于严格
```

#### **2. Non-Guided Planner 的补充作用**

```
优势：
✅ 实时适应环境变化
✅ 不受预计算的引导轨迹限制
✅ 可以发现新的可行路径
✅ 作为"备选方案"
```

---

## 📊 实际运行示例

### **配置**

```yaml
# guidance_planner/config/params.yaml
homotopy:
  n_paths: 4  # 4 个引导轨迹

# mpc_planner_jackalsimulator/config/settings.yaml
t-mpc:
  use_t-mpc++: true  # 启用 T-MPC++
  enable_constraints: true  # 启用拓扑约束
```

### **运行时的求解器分配**

```
总共 5 个并行求解器：

┌─────────────────────────────────────────────────┐
│ Planner 0: Guided (Homotopy Class 1)           │
│   - 有引导轨迹                                   │
│   - 有拓扑约束                                   │
│   - 在引导轨迹附近优化                           │
├─────────────────────────────────────────────────┤
│ Planner 1: Guided (Homotopy Class 2)           │
│   - 有引导轨迹                                   │
│   - 有拓扑约束                                   │
│   - 在引导轨迹附近优化                           │
├─────────────────────────────────────────────────┤
│ Planner 2: Guided (Homotopy Class 3)           │
│   - 有引导轨迹                                   │
│   - 有拓扑约束                                   │
│   - 在引导轨迹附近优化                           │
├─────────────────────────────────────────────────┤
│ Planner 3: Guided (Homotopy Class 4)           │
│   - 有引导轨迹                                   │
│   - 有拓扑约束                                   │
│   - 在引导轨迹附近优化                           │
├─────────────────────────────────────────────────┤
│ Planner 4: Non-Guided (T-MPC++)                │
│   - 无引导轨迹 ✗                                │
│   - 无拓扑约束 ✗                                │
│   - 完全自由优化 ✓                              │
│   - is_original_planner = true                 │
└─────────────────────────────────────────────────┘
```

---

## 🔬 技术细节

### **1. 约束差异**

| 约束类型 | Guided Planner | Non-Guided Planner |
|---------|----------------|-------------------|
| **动力学约束** | ✅ | ✅ |
| **控制输入约束** | ✅ | ✅ |
| **避障约束** | ✅ | ✅ |
| **拓扑约束** | ✅ | ❌ |
| **引导轨迹初始化** | ✅ | ❌ |

### **2. 初始化差异**

**Guided Planner**:
```cpp
// 用引导轨迹初始化
for (int k = 1; k < solver->N; k++)
{
    Eigen::Vector2d cur_position = trajectory_spline.getPoint(k * dt);
    solver->setEgoPrediction(k, "x", cur_position(0));
    solver->setEgoPrediction(k, "y", cur_position(1));
    // ...
}
```

**Non-Guided Planner**:
```cpp
// 使用主求解器的当前解（或默认初始化）
*solver = *_solver;  // 复制主求解器
// 不调用 initializeSolverWithGuidance()
```

### **3. 参数设置差异**

**Guided Planner**:
```cpp
planner.guidance_constraints->setParameters(data, module_data, k);
// data 包含引导轨迹信息
```

**Non-Guided Planner**:
```cpp
planner.guidance_constraints->setParameters(empty_data_, module_data, k);
// empty_data_ 是空的，无引导信息
```

---

## 🎯 总结

### **Non-Guided Planner 的定义**

**Non-Guided Planner（非引导规划器）** 是指：
- ❌ **不使用**引导轨迹初始化
- ❌ **不添加**拓扑约束
- ✅ **只有**基本的安全约束（避障、动力学、控制输入）
- ✅ **完全自由**地在可行空间内优化

### **T-MPC++ 的作用**

T-MPC++ 通过添加一个 Non-Guided Planner，实现：

1. **鲁棒性提升** - 当所有引导轨迹都不可行时，Non-Guided Planner 可能找到解
2. **适应性增强** - 实时适应动态环境变化
3. **创新路径** - 发现引导规划器未考虑的路径
4. **备选方案** - 作为引导规划器的补充

### **配置控制**

```yaml
t-mpc:
  use_t-mpc++: true   # 启用/禁用 Non-Guided Planner
  enable_constraints: true  # 启用/禁用拓扑约束
```

- `use_t-mpc++: false` → 只有 4 个 Guided Planners
- `use_t-mpc++: true` → 4 个 Guided + 1 个 Non-Guided = 5 个

---

**简单来说**：

> **Non-Guided Planner** 就是一个**不受引导轨迹限制的 MPC 求解器**，它可以**自由探索整个可行空间**，作为引导规划器的**补充和备选方案**。

这就是 T-MPC++ 的核心创新！🚀

